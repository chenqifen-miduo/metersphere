# MeterSphere 体验优化产品方案

> 【AI生成】待人工审核确认  
> 文档版本：v1.0  
> 日期：2026-07-17  
> 适用范围：操作列详情入口、测试计划文档化、导航栏项目/组织展示、测试用例模块「测试报告 / 执行用例 / Xmind用例」  
> 状态：待产品 / 技术负责人确认后拆任务实施

---

## 1. 背景与目标

### 1.1 背景（Why）

当前 MeterSphere（Vue3 + Arco Design）在日常测试协作中存在以下痛点：

| 痛点 | 现状 | 影响 |
|------|------|------|
| 详情入口不统一 | 多数列表仅支持点 ID/名称进详情，操作列首项多为「编辑」 | 用户习惯点「详情」查看，易误点编辑；跨模块体验不一致 |
| 测试规划偏「脑图」 | 测试计划详情「测试规划」Tab 为 KityMinder 思维导图 | 与团队既有「Markdown 测试计划文档」工作流脱节，难评审、难归档 |
| 项目切换辨识弱 | 顶栏项目下拉仅约 `50px` 宽，选中名称被 CSS 隐藏，靠 tooltip | 长项目名无法识别；当前组织不直观 |
| 测试报告外置 | 报告多在测试计划报告中心，用例模块无评审/用例 Tab | 用例侧缺少「一键出报告 + 可编辑」闭环 |
| Xmind 与执行用例混用 | 用例导入同时支持 Excel / Xmind，Xmind 导入会解析为功能用例 | 需要「只存文件、在线查看」的场景无法满足 |

### 1.2 目标（What）

1. **操作列统一增加「详情」**：位于「编辑」左侧，行为与点击 ID 进入详情一致；覆盖各业务模块列表。
2. **测试计划详情文档化**：「测试规划」更名为「测试计划」；删除思维导图模式，改为可编辑的 Markdown 测试计划模板（对齐《商城-订单改址-测试计划》结构）。
3. **顶栏项目切换可读**：下拉适配最长约 30 字项目名；组件左移；右侧展示当前组织名称。
4. **用例模块新增「测试报告」Tab**：格式对齐《测试报告-扫码模板…》样例；一键生成、资料自动取数、可编辑；不含「测试依据」、不含「六、附件」；缺陷双图 + 遗留风险自动汇总。
5. **用例 Tab 拆分**：下挂「执行用例」「Xmind用例」；执行用例保留现有能力但导入去掉 Xmind；Xmind用例独立存文件、可打开查看。

### 1.3 非目标（本期不做）

- 不改造接口测试 / 场景自动化报告体系本身（可复用能力，不合并入口）。
- 不把 Xmind用例 Tab 做成「导入后自动转功能用例」（与现有 Xmind 导入解析能力刻意拆开）。
- 不在本期实现测试报告「对外分享链接 / PDF 导出」除非评审明确要求（现有计划报告能力可作二期）。
- 不调整左侧一级菜单信息架构。

---

## 2. 现状代码基线（实现依据）

| 能力 | 关键路径 | 说明 |
|------|----------|------|
| 操作列模式 | `frontend/src/views/**/**Table.vue` 等 `#operation` slot | 统一 `ms-base-table` + 固定右侧操作列；编辑多为 `t('common.edit')` |
| 用例列表 | `caseManagementFeature/components/caseTable.vue` | 点名称/ID → `showCaseDetail` 抽屉；编辑同开详情抽屉 |
| 测试计划列表 | `test-plan/testPlan/components/planTable.vue` | `openDetail` → `TEST_PLAN_INDEX_DETAIL`；操作列有执行/编辑等 |
| 测试规划 Tab | `testPlan/detail/index.vue` → `detail/plan/index.vue` → `ms-minders/testPlanMinder` | i18n：`testPlan.plan` =「测试规划」；后端 `getPlanMinder` / `editPlanMinder` |
| 顶栏项目切换 | `components/pure/navbar/index.vue` | `w-[50px]` + 隐藏 `.arco-select-view-value`；数据来自 `appStore.projectList` |
| 组织上下文 | `store/modules/app`：`currentOrgId`、`orgList` | 顶栏未展示组织名 |
| 用例顶栏 Tab | `router/routes/modules/caseManagement.ts` + `ms-top-menu` | `meta.isTopMenu` →「用例」「评审」 |
| 用例导入 | `caseManagementFeature/components/import/*` | Excel / Xmind 校验与导入 API 已存在 |
| 可复用报告 | `test-plan/report/**`、`MsRichText` | 计划报告布局/卡片/编辑可参考；富文本编辑可用 |
| 文件管理 | `project-management/fileManagement` | Xmind 文件存储可参考项目文件能力 |

技术栈：Vue 3 + Arco Design Vue + Pinia + vue-router + vue-i18n；脑图为 `@7polo/kityminder-core`。

---

## 3. 需求分项方案

### 3.1 【P0】操作列新增「详情」按钮

#### 3.1.1 动机-行为-呈现

- **Why**：降低误入编辑；统一「先看后改」路径。
- **How**：列表行操作列在「编辑」左侧增加「详情」；点击后走与「点击 ID / 名称进详情」相同逻辑与权限。
- **What**：文案「详情」；权限与当前「查看详情」一致（通常为模块 `READ`，不要求 `UPDATE`）。

#### 3.1.2 覆盖范围（「所有模块」落地清单）

以「存在操作列且存在详情入口」的业务列表为准：

| 模块 | 建议改动文件（前端） | 详情行为对齐 |
|------|----------------------|--------------|
| 测试用例-功能用例 | `caseTable.vue`（及计划内关联用例表若有编辑） | 同点 ID → 详情抽屉 |
| 测试用例-评审 | `caseReview/.../reviewTable.vue` | 同点 ID → 评审详情 |
| 测试计划 | `planTable.vue` | 同 `openDetail`；计划组按现有详情规则 |
| 缺陷 | `bug-management/index.vue` | 同点 ID → 缺陷详情抽屉 |
| 接口定义 / 接口用例 / 场景 | 对应 `apiTable.vue` / `caseTable.vue` / `scenarioTable.vue` | 同现有点 ID/名称进详情 |
| 测试计划报告列表、接口报告列表 | `reportList.vue` 等 | 若操作列有「编辑」则同样加「详情」 |

**明确排除**：纯配置表（无详情页）、仅「更多」无编辑的操作列、系统设置类表格（无「资源详情」语义）。

#### 3.1.3 交互规则

1. 顺序：**详情 | 编辑 | …**（其后保持各模块原有按钮与「更多」）。
2. 无编辑权限时：仍可显示「详情」（有查看权限时）；无查看权限则整列按现有逻辑隐藏。
3. 宽度：操作列 `width` 按模块现有值上调约 40–60px，避免挤出。
4. i18n：优先复用已有 `*.detail` / 新增 `common.detail` =「详情」，中英繁统一。

#### 3.1.4 验收标准

- [ ] 上述清单模块操作列「详情」在「编辑」左侧。
- [ ] 点击「详情」与点击 ID 进入同一页面/抽屉、同一权限校验。
- [ ] 无编辑权限用户仍可点「详情」查看（在具备 READ 时）。

---

### 3.2 【P0】测试计划详情：「测试规划」→「测试计划」+ Markdown 模板

#### 3.2.1 动机-行为-呈现

- **Why**：用结构化测试计划文档替代脑图，便于评审、归档、与外部文档对齐。
- **How**：Tab 文案变更；移除 `MsTestPlanMinder`；改为 Markdown/富文本编辑器 + 标准章节模板；支持保存、自动带出部分字段。
- **What**：Tab 名「测试计划」；内容为可编辑文档，章节对齐样例文档。

#### 3.2.2 模板结构（对齐样例，字段可空）

以 `商城-订单改址-测试计划-20260717.md` 为章节骨架：

1. 文档信息（编号、版本、所属项目、测试模块、编制日期、编制人、审核人）
2. 项目背景
3. 测试目标
4. 测试范围（范围内 / 范围外）
5. 测试策略
6. 测试重点（主流程 / 规则与边界 / 权限与安全）
7. 测试环境与数据
8. 准入、暂停与退出标准
9. 测试进度
10. 人员与职责
11. 缺陷管理
12. 风险与应对
13. 测试交付物
14. 审批记录

**系统自动填充（首次生成或一键重置模板时）**：

| 字段 | 数据来源 |
|------|----------|
| 所属项目 | 当前项目名称 |
| 测试模块 / 文档标题建议 | 测试计划名称 |
| 编制日期 | 当天日期 |
| 编制人 | 当前用户名 |
| 文档编号 | 建议规则：`TP-{projectCode或缩写}-{planNum}-{yyyyMMdd}`（规则需评审确认） |

其余章节保留空表格 /「待填写」占位，由用户编辑。

#### 3.2.3 交互设计

1. 进入「测试计划」Tab：若该计划尚无文档内容 → 自动套用模板；若已有 → 展示已存内容。
2. 工具栏：**保存**（必选）、**预览**（建议）、**重置为模板**（二次确认，覆盖当前内容）。
3. 编辑器：优先复用 `MsRichText`；若需纯 Markdown 源码切换，可提供「源码 / 所见即所得」二态（实现阶段二选一，产品确认）。
4. 权限：查看 `PROJECT_TEST_PLAN:READ`；编辑/保存与计划编辑权限对齐（建议 `READ+UPDATE`）。
5. **删除思维导图**：前端不再渲染 minder；后端 minder 接口可保留一段时间但 UI 不可达（二期再清理表结构，避免一次大迁移）。

#### 3.2.4 数据与接口（建议）

| 项 | 建议 |
|----|------|
| 存储 | 新表或计划扩展字段：`test_plan_document`（plan_id、content、content_type、update_user、update_time） |
| API | `GET /test-plan/{id}/document`、`POST /test-plan/{id}/document`（保存） |
| 兼容 | 旧 minder 数据不迁移为文档；首次进入显示空白模板即可 |

#### 3.2.5 验收标准

- [ ] Tab 文案为「测试计划」（中英繁）。
- [ ] 页面无思维导图编辑器与保存脑图入口。
- [ ] 可见完整模板章节；可编辑并持久化；刷新后内容保留。
- [ ] 自动填充字段正确；重置模板需确认。

---

### 3.3 【P1】项目切换下拉加长 + 左移 + 右侧显示组织

#### 3.3.1 动机-行为-呈现

- **Why**：项目名可读；组织上下文可见，减少切错项目。
- **How**：加宽下拉并展示选中名称；在顶栏右区将其左移；右侧展示当前组织名。
- **What**：约 30 汉字可视宽度；组织名只读展示。

#### 3.3.2 布局方案（推荐）

当前：`Logo | TopMenu(居中) | [项目▼] 图标区…`（项目在最右前）。

**目标**：

```
Logo | TopMenu | …… | [项目名称下拉 ≈30字宽] [当前组织：xxx] | 通知/任务/帮助/…
```

- 「左移」含义：在 `right-side` 内，项目选择仍为该区域最左侧控件，但其**可视主体变为项目名**（不再是 50px 箭头），视觉重心左移；组织文本紧随其后。
- 不占用 Logo 区；不改变 TopMenu 居中逻辑（避免大改导航栅格）。

若评审要求项目选择靠近 Logo，可作为备选方案 B（改动更大，需评估 TopMenu 宽度）。

#### 3.3.3 规格

| 项 | 规格 |
|----|------|
| 下拉可视宽度 | 按中文字号约 14px，30 字 ≈ `30 * 14 ≈ 420px`；建议 `min-width: 200px; max-width: 420px`（或固定 `w-[420px]`） |
| 展示 | 取消隐藏 `.arco-select-view-value`；超长省略号 + tooltip 全文 |
| 组织展示 | 文案：「当前组织」+ `orgList` 中匹配 `currentOrgId` 的 `name`；超长省略 |
| 交互 | 组织名本期只读（切换组织仍走现有组织入口） |
| 响应式 | 小屏可缩短为 `max-width: 240px`，组织名可仅图标+tooltip |

改动主文件：`frontend/src/components/pure/navbar/index.vue`；样式与 i18n（`settings.navbar.*`）。

#### 3.3.4 验收标准

- [ ] 30 字项目名主体可见（不依赖 hover 才知道当前项目）。
- [ ] 项目下拉右侧可见当前组织名称。
- [ ] 切换项目行为与现网一致。

---

### 3.4 【P0】测试用例模块 —「测试报告」Tab

#### 3.4.1 动机-行为-呈现

- **Why**：在用例模块直接产出可编辑测试报告，对齐团队报告模板。
- **How**：在「评审」右侧增加顶栏 Tab「测试报告」；提供报告列表 + 详情编辑；「一键生成」从系统取数填充。
- **What**：报告结构见下；支持二次编辑与保存。

#### 3.4.2 信息架构

```
测试用例模块顶栏：
  用例 | 评审 | 测试报告（新增）
```

路由建议：`/case-management/testReport`，`meta.isTopMenu: true`，locale：`menu.caseManagement.testReport` =「测试报告」。

页面结构建议：

1. **列表页**：报告名称、关联范围（版本/计划/时间）、生成人、更新时间、操作（详情｜编辑｜删除｜…）。
2. **详情/编辑页**：按模板分节渲染；支持编辑模式。

#### 3.4.3 报告输出格式（对齐样例，并按需求裁剪）

| 章节 | 是否输出 | 内容来源 / 规则 |
|------|----------|-----------------|
| 一、版本概览 | ✅ | 产品/系统名称、产品经理、开发负责人、测试负责人、计划测试周期、实际测试周期、上线计划日期 → **可编辑**；生成时尽量带出项目名、当前用户为测试负责人默认值 |
| 二、测试范围与目的 | ✅ 仅「1. 测试内容」 | 可编辑文本；**不输出「2. 测试依据」**（系统无记录） |
| 三、质量评估 | ✅ | 见下 |
| 　3.1 测试执行统计 | ✅ | 按项目（或用户选定范围）汇总功能用例执行：总数/通过/失败/阻塞/执行率/通过率；说明文案可写死与样例一致 |
| 　3.2 缺陷统计与分析 | ✅ | **图1**：按「处理人 × 处理状态」维度柱状图；**图2**：按「缺陷类型」饼图 |
| 四、遗留问题与风险 | ✅ | **自动**：汇总执行结果为「阻塞」「失败」的用例（编号+名称+结果）；用户可再手工补充说明 |
| 五、测试结论与建议 | ✅ | 结论、建议 → 可编辑；生成时可给默认结论草稿（如通过率=100% 则「测试通过」） |
| 六、附件 | ❌ | **删除整节**（不展示用例/缺陷外链占位） |
| 页脚 | ✅ | 报告撰写人、日期（默认当前用户与当天） |

> 通过率计算说明与样例对齐：`(通过数 / (用例总数 - 阻塞数 - 失败数)) * 100%`（需在 UI 旁注释；若分母为 0 显示 `-`）。

#### 3.4.4 「一键生成报告」流程

```
用户点击「一键生成报告」
  → 选择数据范围（建议：当前项目 + 可选测试计划 / 时间范围 / 版本）【待确认】
  → 后端聚合：用例执行统计、缺陷统计、失败/阻塞用例列表
  → 前端套模板生成可编辑报告草稿并落库
  → 进入编辑页，用户改文字后保存
```

**自动取数映射（建议）**：

| 报告块 | 系统数据 |
|--------|----------|
| 执行统计 | 功能用例最新执行结果（或选定测试计划内执行结果） |
| 图1 柱状图 | 缺陷：处理人（handler）× 状态（status）计数 |
| 图2 饼图 | 缺陷：类型（bug type / 自定义字段「缺陷类型」）计数 |
| 遗留风险 | `lastExecResult ∈ {BLOCKED, ERROR/FAILED}` 的用例列表 |

图表：前端可用项目已有图表组件（工作台缺陷图可参考 `workbench/.../defectMemberBar.vue` 等）或 ECharts。

#### 3.4.5 编辑与权限

- 生成后全文可编辑（含表格数字是否允许改——建议：**统计表与图表默认只读快照，允许「刷新统计」；文字章节可编辑**。若产品要求数字也可改，则改为纯快照可改写）。
- 权限建议：新权限点 `FUNCTIONAL_REPORT:READ / READ+ADD / READ+UPDATE / READ+DELETE`，或先复用用例模块权限以降成本（评审确认）。

#### 3.4.6 与现有「测试计划报告」关系

| | 计划报告（已有） | 用例模块测试报告（本期） |
|--|------------------|--------------------------|
| 入口 | 测试计划 / 报告菜单 | 测试用例顶栏 Tab |
| 形态 | 卡片化执行分析 | 固定业务模板（版本概览…结论） |
| 复用 | 统计 API、PDF/分享可二期复用 | 本期独立存储与 UI |

#### 3.4.7 验收标准

- [ ] 「评审」右侧出现「测试报告」Tab。
- [ ] 一键生成后章节齐全且符合裁剪规则（无测试依据、无附件）。
- [ ] 图1/图2 维度正确；遗留风险含阻塞+失败用例。
- [ ] 报告可编辑保存；列表可再次打开。

---

### 3.5 【P0】用例 Tab：执行用例 / Xmind用例

#### 3.5.1 动机-行为-呈现

- **Why**：把「可执行功能用例」与「思维导图文件资产」分开管理。
- **How**：在「用例」路由页内增加二级 Tab；导入能力拆分。
- **What**：二级 Tab「执行用例」「Xmind用例」。

#### 3.5.2 信息架构

```
顶栏「用例」
  └─ 页内二级 Tab
        ├─ 执行用例  → 现有功能用例列表/模块树/新建/导入(仅 Excel 等非 Xmind)
        └─ Xmind用例 → Xmind 文件库：上传、列表、在线查看
```

默认进入「执行用例」。二级 Tab 切换不改变顶栏选中态。

#### 3.5.3 执行用例

- 内容 = **当前「用例」页全部能力**（模块树、表格、新建、导出、评审结果列等）。
- **导入按钮**：删除「Xmind 导入」选项（`exportCaseModal` 中仅保留 Excel；相关文案/模板下载入口同步隐藏）。
- 后端 Xmind 导入 API 可保留（兼容旧脚本），前端不可达即可。

#### 3.5.4 Xmind用例

| 能力 | 说明 |
|------|------|
| 上传/导入 | 选择 `.xmind` 文件上传至项目；记录名称、大小、上传人、时间、所属模块（可选） |
| 存放 | 走文件存储（复用文件管理或新业务表 + MinIO/本地存储，与现有附件一致） |
| 列表 | 名称、更新时间、操作：打开查看 / 下载 / 重命名 / 删除 |
| 打开查看 | 系统内预览：优先评估嵌入开源 Xmind/脑图查看器；若工期紧，**MVP：下载 + 新窗口提示**，二期在线预览（需在评审中拍板） |
| 与功能用例关系 | **不自动解析为用例**；用户需要结构化用例时仍用「执行用例」的 Excel 导入或手工新建 |

#### 3.5.5 验收标准

- [ ] 「用例」下可见「执行用例」「Xmind用例」两个二级 Tab。
- [ ] 执行用例导入无 Xmind 选项。
- [ ] Xmind用例可上传、列表展示、删除；可打开查看（按 MVP 定义验收）。

---

## 4. 跨需求依赖与优先级

| 优先级 | 项 | 依赖 |
|--------|----|------|
| P0 | 3.1 详情按钮 | 无，可并行 |
| P0 | 3.2 测试计划文档化 | 新存储/API；与 minder 解耦 |
| P0 | 3.4 测试报告 Tab | 统计聚合 API；图表 |
| P0 | 3.5 执行/Xmind 拆分 | 导入改造；文件存储 |
| P1 | 3.3 项目/组织顶栏 | 独立，可穿插 |

建议实施顺序：`3.1` → `3.3`（体验快赢）→ `3.5` → `3.2` → `3.4`（3.4 最重）。

---

## 5. 待产品确认事项（阻塞实现前需拍板）

1. **「所有模块」边界**：是否包含系统设置/组织/项目管理下的表格？方案默认仅业务资源列表。
2. **测试计划文档编辑器**：富文本（MsRichText）还是纯 Markdown？是否需要导出 `.md` / Word？
3. **旧思维导图数据**：是否需要只读迁移提示，还是直接丢弃展示？
4. **测试报告数据范围**：按「整个项目」还是必须绑定「测试计划/版本/时间」？一键生成弹窗字段集合。
5. **报告内统计数字**：生成后只读快照 + 刷新，还是允许手工改数字？
6. **缺陷「类型」字段**：系统内对应哪个字段（模板自定义字段 key）？处理人取 `handleUser` 还是创建人？
7. **Xmind 在线预览**：本期 MVP 是否接受「仅下载查看」？
8. **顶栏方案**：确认采用「右区加宽」还是「移到 Logo 旁」。

---

## 6. 风险与注意

- **业务逻辑 / 权限**：新报告、文档保存、Xmind 文件读写涉及鉴权与项目隔离，实现后需**人工审核**权限矩阵。
- **通过率公式**与平台其它处「通过率」定义可能不一致，需在报告内注明，避免与计划报告口径混淆。
- **操作列宽度**在多按钮模块（计划：执行|详情|编辑|…）可能换行，需按模块微调。
- **移除测试规划脑图**会影响依赖脑图关联用例的用户；需发版说明与操作迁移指引（改为在计划内「功能用例」等 Tab 关联）。

---

## 7. 里程碑建议（供排期）

| 阶段 | 内容 | 粗估 |
|------|------|------|
| M1 | 详情按钮全模块 + 顶栏项目/组织 | 2–3 人日 |
| M2 | 执行用例/Xmind 拆分 + 导入去 Xmind | 3–5 人日（含在线预览则 +3～5） |
| M3 | 测试计划 Markdown 文档化 | 3–5 人日 |
| M4 | 测试报告 Tab + 一键生成 + 双图 + 编辑 | 8–12 人日 |

---

## 8. 附录

### 8.1 参考文档

- 测试计划模板：`商城-订单改址-测试计划-20260717.md`
- 测试报告样例：`测试报告-扫码模板支持自定义跳转第三方小程序（百年糊涂数据迁移场景）.md`

### 8.2 关键前端锚点速查

- 顶栏：`frontend/src/components/pure/navbar/index.vue`
- 用例顶栏路由：`frontend/src/router/routes/modules/caseManagement.ts`
- 测试计划详情 Tab：`frontend/src/views/test-plan/testPlan/detail/index.vue`
- 测试规划脑图：`frontend/src/views/test-plan/testPlan/detail/plan/index.vue`
- 用例导入：`frontend/src/views/case-management/caseManagementFeature/components/import/`

---

**文档结束。确认第 5 节待决项后，可拆为 `docs/task/` 下可执行任务单。**

---

## 9. 已拆解执行任务（2026-07-17）

已按 `docs/task/community_rebuild` 体例拆解至：

**目录**：[`docs/task/experience_optimize/`](../task/experience_optimize/task000-实施总览与依赖关系.md)

| 任务 | 对应方案章节 |
|------|----------------|
| [task000](../task/experience_optimize/task000-实施总览与依赖关系.md) | 总览 / 依赖 / 默认决策 |
| [task001](../task/experience_optimize/task001-P0-操作列详情按钮.md) | §3.1 |
| [task002](../task/experience_optimize/task002-P1-顶栏项目组织展示.md) | §3.3 |
| [task003](../task/experience_optimize/task003-P0-执行用例与Xmind拆分.md) | §3.5 |
| [task004](../task/experience_optimize/task004-P0-测试计划文档数据模型与API.md) | §3.2.4 |
| [task005](../task/experience_optimize/task005-P0-测试计划文档前端替换脑图.md) | §3.2 |
| [task006](../task/experience_optimize/task006-P0-测试报告数据模型与聚合API.md) | §3.4 数据/API |
| [task007](../task/experience_optimize/task007-P0-测试报告Tab与一键生成前端.md) | §3.4 前端 |

方案 §5 待决项已在 task000 §4 给出**默认产品决策**；产品推翻后先改总览再改对应 task。
