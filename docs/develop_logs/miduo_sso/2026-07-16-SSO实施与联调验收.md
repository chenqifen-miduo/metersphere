# Miduo SSO 联调验收记录

> **日期**：2026-07-16  
> **状态**：代码已落地，待米多侧凭据联调  
> **标注**：【AI生成】实施记录，验收勾选需人工在联调后更新

## 已实现范围

| Task | 内容 | 代码状态 |
|------|------|----------|
| task001 | `MiduoSsoProperties` + Application 注册 | ✅ |
| task002 | `MiduoSsoClient` HMAC + validate/refresh/revoke/bridge | ✅ |
| task003 | state / callback / wecom 匹配 / Shiro Session | ✅ |
| task004 | Redis sessionToken + logout revoke | ✅ |
| task005 | Shiro Extender + CDS `/auth/` + Vite proxy | ✅ |
| task006 | MiduoCallback 页 + WHITE_LIST | ✅ |
| task007 | refreshIfNeeded + `needMiduoReauth` 字段 + 前端跳 bridge | ✅ |
| task008 | status 门禁 + nonce + 日志脱敏 | ✅ |
| task009 | `/#/login` 自动跳桥 + `/#/login/admin` 账密入口 | ✅ |

## 环境变量清单（待填写）

```properties
miduo.sso.enabled=true
miduo.sso.base-url=
miduo.sso.app-code=
miduo.sso.app-secret=
miduo.sso.redirect-uri=https://{host}/#/sso/miduo/callback
miduo.sso.shortcut-id=
miduo.sso.organization-id=
```

## 登录入口约定（2026-07-16 调整）

| 路径 | 行为 |
|------|------|
| `/#/login` | 自动跳转米多登录桥；SSO 不可用时仅保持加载态，**不**提示、**不**展示管理员入口 |
| `/#/login/admin` | 账密 / LDAP / 扫码登录；**仅**可通过手动输入地址进入 |

## 联调检查

- [ ] 米多确认单四项已对齐
- [ ] 灰度 `/front/auth/miduo/status` 返回 JSON
- [ ] 工作台跳入 / `/#/login` 自动跳桥可完成 callback
- [ ] `/#/login/admin` 可账密登录
- [ ] 未同步用户拒绝并提示
- [ ] logout 后 revoke 生效
- [ ] refresh 失败 message/`needMiduoReauth` 可跳 bridge
- [ ] Agent API 不受影响

## 运维注意

1. CDS `cds.path-prefixes` 须含 `/auth/`
2. Secret 仅环境变量 / Nacos，禁止入库
3. 工作台入口须保证米多回传的 `state` 与 MS 预先生成一致（bridge-url 已带 state）
