# 开发摘要日志 — task012 Cursor 接入与工作流规则

> **日期**：2026-07-07  
> **任务**：[task012-P1-Cursor接入与工作流规则.md](../../task/metersphere_agent/task012-P1-Cursor接入与工作流规则.md)  
> **前置**：task011 ✅  
> **状态**：✅ 已完成（端到端实测待完成）

---

## 1. 本次目标

团队 Cursor 接入指南、MCP 配置模板、Agent 工作流规则。

---

## 2. 交付物

| 文件 | 说明 |
|------|------|
| `docs/task/metersphere_agent/cursor-onboarding.md` | 接入指南 |
| `.cursor/rules/metersphere-agent.mdc` | 工作流规则 |
| `.cursor/mcp.json.example` | MCP 配置模板（无真实 Token） |

---

## 3. 工作流规则要点

1. 模糊表述 → 先 `list_modules`  
2. 检查 `matchedBy` / `warnings`；>20 条先摘要  
3. 执行前确认 `testPlanCaseId`  
4. 外部执行后逐条 `submit_functional_result`  
5. 汇总通过/失败数  

---

## 4. Cursor 配置（本地包）

```json
{
  "mcpServers": {
    "metersphere": {
      "command": "node",
      "args": ["metersphere-mcp/dist/index.js"],
      "env": { "MS_BASE_URL": "...", "MS_AGENT_TOKEN": "...", ... }
    }
  }
}
```

---

## 5. 待验证

- [ ] 复制 `mcp.json.example` → `mcp.json` 并重启 Cursor  
- [ ] 模块检索 / 消歧 / 回写三个端到端场景  

---

## 6. 变更文件

| 文件 | 说明 |
|------|------|
| `docs/task/metersphere_agent/cursor-onboarding.md` | 新建 |
| `.cursor/rules/metersphere-agent.mdc` | 新建 |
| `.cursor/mcp.json.example` | 新建 |
