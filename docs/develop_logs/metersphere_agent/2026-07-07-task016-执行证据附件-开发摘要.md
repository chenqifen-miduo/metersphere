# 开发摘要日志 — task016 执行证据附件

> **日期**：—  
> **任务**：[task016-P2-执行证据附件.md](../../task/metersphere_agent/task016-P2-执行证据附件.md)  
> **前置**：task007 ✅  
> **状态**：⏳ 待开始

---

## 1. 计划目标

支持 Agent 上传截图/附件，关联到测试计划执行历史，便于失败追溯。

---

## 2. 计划实现

| 组件 | 说明 |
|------|------|
| `POST .../attachment/upload` | 上传 PNG 等 |
| `agent_exec_attachment` 表 | 关联 exec_history / exec_log |
| submit 扩展 | `attachmentIds` 或 base64 |
| MCP Tool | `upload_execution_attachment` |

复用 MeterSphere 现有 MinIO / 本地文件存储。

---

## 3. 约束

- 单文件最大 5MB  
- 单次 submit 最多 10 个附件  

---

## 4. 依赖

- task007 计划内回写  
- task013 计划外审计（可选关联 exec_log）  

---

## 5. 开发日志

*实施完成后在此补充产出、验证与变更文件清单。*
