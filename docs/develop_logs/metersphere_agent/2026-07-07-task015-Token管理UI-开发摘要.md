# 开发摘要日志 — task015 Token 管理 UI

> **日期**：—  
> **任务**：[task015-P2-Token管理UI.md](../../task/metersphere_agent/task015-P2-Token管理UI.md)  
> **前置**：task003 ✅  
> **状态**：⏳ 待开始

---

## 1. 计划目标

系统设置新增「Agent 集成 → Token 管理」页面，替代 P0 手工 SQL 创建 Token。

---

## 2. 计划实现

### 后端

| API | 说明 |
|-----|------|
| `POST /api/agent/token/add` | 创建，明文仅展示一次 |
| `GET /api/agent/token/page` | 分页列表 |
| `POST /api/agent/token/update` | 更新 scope/过期/启用 |
| `GET /api/agent/token/delete/{id}` | 删除 |

走 Session 认证，**不走** AgentTokenFilter。

### 前端

- `frontend/src/views/setting/system/agentIntegration/index.vue`  
- `frontend/src/api/modules/setting/agentIntegration.ts`  

---

## 3. 依赖

- task003 `AgentTokenService` 可扩展 `createToken()`  

---

## 4. 开发日志

*实施完成后在此补充产出、验证与变更文件清单。*
