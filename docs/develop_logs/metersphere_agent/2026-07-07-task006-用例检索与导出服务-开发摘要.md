# 开发摘要日志 — task006 用例检索与导出服务

> **日期**：2026-07-07  
> **任务**：[task006-P0-用例检索与导出服务.md](../../task/metersphere_agent/task006-P0-用例检索与导出服务.md)  
> **前置**：task005 ✅  
> **状态**：✅ 已完成

---

## 1. 本次目标

实现 search / get / modules 业务链路，一次返回 steps 与 testPlanCaseId。

---

## 2. 服务职责

| 类 | 方法 |
|----|------|
| `AgentFunctionalCaseSearchService` | `search`、`getById`、`listModules` |
| `AgentFunctionalCaseExportService` | `enrichWithSteps`（批量拉详情，避免 N+1） |

---

## 3. 复用现有 Service

| 场景 | 复用 |
|------|------|
| 用例库检索 | `FunctionalCaseService.getFunctionalCasePage` |
| 计划内检索 | `TestPlanFunctionalCaseService.getFunctionalCasePage` |
| 用例详情 | `FunctionalCaseService.getFunctionalCaseDetail` |
| 模块树 | `FunctionalCaseModuleService.getTree` |

---

## 4. 过滤映射

| Agent filters | 内部 filter key |
|--------------|----------------|
| `priority` | `caseLevel`（自定义字段 functional_priority） |
| `lastExecuteResult` | `lastExecuteResult` / `lastExecResult`（计划内） |

---

## 5. 关键行为

- 传 `testPlanId` 时每条 case 带 `testPlanCaseId`  
- `includeSteps=true` 批量 enrich  
- Text 模式虚拟步骤 + `TEXT_MODE_CONVERTED` warning  

---

## 6. 变更文件

| 文件 | 说明 |
|------|------|
| `service/AgentFunctionalCaseSearchService.java` | 新建 |
| `service/AgentFunctionalCaseExportService.java` | 新建 |
