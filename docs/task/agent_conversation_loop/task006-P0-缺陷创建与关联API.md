# task006 - P0 缺陷创建与关联 API

> **阶段**：P0  
> **前置依赖**：task001、task003  
> **关联方案**：§7.5

---

## 2. 任务清单

- [x] `bug/create`、`bug/relate-case`（`BUG_WRITE`）  
- [x] 支持 `customFields` / `caseId` / `testPlanId` / `testPlanCaseId`  
- [x] 审计：`BUG_CREATE`、`BUG_RELATE_CASE`  

---

## 4. 任务状态

| 字段 | 值 |
|------|-----|
| 状态 | **代码已核对**；运行时待 task010（必填字段可能 WARN） |
| 备注 | 字段发现 API → task012 |
