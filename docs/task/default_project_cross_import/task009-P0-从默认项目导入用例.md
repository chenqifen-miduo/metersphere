# task009 - P0 从默认项目导入用例

> **阶段**：S8  
> **预估工期**：4–5 天  
> **前置依赖**：task003（文件夹树）、task008（枢纽有数据；无枢纽时可用种子数据联调）  
> **阻塞任务**：task011  
> **关联方案**：§3.5、§3.10  
> **提测**：T7、T7b、T8、T15（图片若涉及）、T16  

---

## 1. 任务目标

用例导入弹窗：Excel Tab 保持 +「从【米多公司默认项目】导入」Tab；仅从默认项目选树导入；冲突 SKIP/OVERWRITE；批量异步约束。

---

## 2. 任务清单

### 2.1 FE

- [ ] 改造导入弹窗双 Tab；**无**任意业务项目选择器  
- [ ] 加载默认项目模块树（文件夹/模块/未规划/全部）  
- [ ] 勾选状态机：`none|cases|module|folder|all|unplanned` → 控制导入按钮（#10）  
- [ ] 冲突弹窗：跳过/覆盖 + 全部跳过/全部覆盖  
- [ ] 异步任务进度 UI；成功提示复制边界  

### 2.2 BE

- [ ] `POST /functional/case/import/from-default-project`（强制 source=默认项目）  
- [ ] 选择模式 + `SKIP`/`OVERWRITE`  
- [ ] 复制范围：基本信息 + 前置/备注/步骤；未评审、未执行；禁止附件与一切关联  
- [ ] OVERWRITE：只改正文；保留目标附件/关联  
- [ ] 单次 ≤**500**；超限拒绝  
- [ ] **异步任务** + 进度；等待上限 **5 分钟**；失败 **整单回滚**  
- [ ] 可选审计 `imported_from_hub_case_id`；**不**写 hub map  

### 2.3 权限

- [ ] 默认项目 `FUNCTIONAL_CASE:READ`；目标 `IMPORT` 或 `ADD`  

---

## 3. 验收标准

- [ ] T7 / T7b / T8 通过  
- [ ] 无法选择其它业务项目作为导入源  
- [ ] T16：导入后修改业务副本，枢纽不变  

---

## 4. 任务状态

| 字段 | 值 |
|------|-----|
| 状态 | **代码已完成，待环境验收** |
| 备注 | 可与 task010 并行开发，联调依赖枢纽数据 |
