# task003 - P1 用例文件夹类型与左树

> **阶段**：S2  
> **预估工期**：2 天  
> **前置依赖**：无（可与 task005–007 并行）  
> **阻塞任务**：task004、task009  
> **关联方案**：§3.3  
> **提测**：T5  

---

## 1. 任务目标

在 `functional_case_module` 上扩展文件夹类型；左树提供「创建文件夹」入口；文件夹可挂用例；删除行为与现有删模块一致（级联）。

---

## 2. 任务清单

- [ ] DB：`type`/`module_type`（`MODULE` 默认 / `FOLDER`）；旧数据迁移默认 `MODULE`  
- [ ] 预留或同步加 `ref_project_id`（可与 task004 同迁移）  
- [ ] BE：创建文件夹 API；树查询返回 type；删除复用/对齐 `FunctionalCaseModuleService#deleteModule` **级联**  
- [ ] FE：`index.vue` 在 expand 与 ➕ 之间插入文件夹按钮 + PopConfirm  
- [ ] FE：`caseTree.vue` 按 type 区分图标；参与展开收起  
- [ ] 删除二次确认文案明示「级联删除子树及用例」  
- [ ] i18n：`createFolder` / 成功与删除提示  

---

## 3. 验收标准

- [ ] T5：文件夹下可直接建用例、可加子模块；展开收起正常  
- [ ] 删非空文件夹：子节点与挂载用例均删除（与删模块一致）  

---

## 4. 任务状态

| 字段 | 值 |
|------|-----|
| 状态 | **代码已完成，待环境验收** |
| 备注 | 不引入第二套资源权限体系 |
