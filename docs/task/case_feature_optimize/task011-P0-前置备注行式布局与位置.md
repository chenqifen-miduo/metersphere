# task011 - P0 前置备注行式布局与位置

> **阶段**：P0（详情体验二期）  
> **预估工期**：0.5 人日  
> **前置依赖**：无  
> **阻塞任务**：无（建议先于 task012 改 DOM）  
> **关联总览**：[task000](task000-实施总览与依赖关系.md)  
> **关联方案**：[体验优化方案 v1.3.1](../../summary/MeterSphere-功能用例详情-体验优化方案-2026-07-22.md) §4.2  
> **任务状态**：进行中（前端已合入核心改动，待联调）

---

## 1. 任务目标

详情 Tab 只读态将前置、备注改为「标签：内容」行式布局；**备注移到前置正下方**；`StepDescription` 在备注下方；编辑仍纵向富文本 + 统一「内容编辑」态（保持现网）。

---

## 2. 现状分析

| 项 | 说明 |
|----|------|
| 文件 | `tabDetail.vue` |
| 当前顺序 | 前置 → StepDescription/STEP|TEXT → **备注** → 附件 |
| 当前样式 | 纵向 form-item，标签在上 |

---

## 3. 任务清单

### 3.1 DOM 顺序

- [ ] 调整为：`precondition` → `description(备注)` → `StepDescription` → STEP/TEXT 内容 → …  

### 3.2 只读 / 编辑样式

- [ ] 只读：行式「前置：…」「备注：…」，空值 `-`  
- [ ] 编辑：仍纵向 `MsRichText`；「内容编辑」统一编辑态保持现状  
- [ ] i18n 标签可复用现有 key，样式层加冒号  

---

## 4. 涉及文件

| 文件 | 改动 |
|------|------|
| `tabContent/tabDetail.vue` | 顺序与只读样式 |
| 可选抽小组件 | `CaseFieldInline`（非必须） |

---

## 5. 测试用例

| 场景 | 预期 |
|------|------|
| 只读打开详情 | 前置在上、备注紧随、再是 StepDescription |
| 点内容编辑 | 纵向富文本可编辑保存 |
| 空备注 | 显示 `-` |

---

## 6. 验收标准

- [ ] 只读行式且顺序正确  
- [ ] 编辑态与现网统一编辑行为一致  
- [ ] 保存后字段不错乱  
