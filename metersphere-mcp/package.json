{
  "name": "@midoo/metersphere-mcp",
  "version": "0.1.0",
  "description": "MCP server for MeterSphere Agent API - thin HTTP wrapper for Cursor and other MCP clients",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "metersphere-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "metersphere",
    "mcp",
    "cursor",
    "testing",
    "functional-case"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
